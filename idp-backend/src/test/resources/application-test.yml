spring:
    jpa:
      database: H2
      database-platform: org.hibernate.dialect.H2Dialect
    datasource:
      generate-unique-name: true
      url: jdbc:h2:file:./h2/primaIDP;CIPHER=AES
      username: sa
      password: encrypted sa
      driver-class-name: org.h2.Driver
    h2:
      console:
        enabled: false
        settings:
          trace: true


keystore:
  path: classpath:keys/keystore-test.jks
  alias: sgi.prima-solutions.com
  password: Bourso$17


test:
  cl-prima-client-dev:
    private-key: classpath:keys/cl.prima.client.dev.key


logging:
  file: logs/primaidp-test.log
  level:
    ROOT: ERROR
    org.opensaml: INFO
    org.springframework.core: INFO
    org.springframework.security.saml: INFO

liquibase:
  change-log: classpath:database/db-changelog-test.yaml
  contexts: test
  drop-first: true