databaseChangeLog:
  - changeSet:
      id: 1
      author: mlyahmed
      changes:
        - createTable:
            tableName: PERSON_BY_EMAIL
            columns:
              - column:
                  name: PERSON_ID
                  type: varchar(300)
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: EMAIL
                  type: varchar(300)
                  constraints:
                    nullable: false